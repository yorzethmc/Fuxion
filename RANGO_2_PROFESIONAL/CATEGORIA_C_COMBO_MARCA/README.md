# Fusión Culinaria - Combo Marca

Sitio Vite + React listo para despliegue estático en Vercel o Netlify.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

## Configuración

- WhatsApp: `50660224371` en `src/App.jsx`.
- SEO base: `index.html`.
- Netlify fallback: `_redirects`.
- Vercel headers: `vercel.json`.
